- if controller_name != 'sessions'
	= link_to "Σύνδεση", new_session_path(resource_name), class: "devise-links"
	%br

-# if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
	= link_to "Ξεχάσατε τον κωδικό σας;", new_password_path(resource_name), class: "devise-links"
	%br
